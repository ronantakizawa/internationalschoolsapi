name: Secrets example
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          npm install
          npm run-script build
        env: 
          dbURI: ${{ secrets.dbURI }}